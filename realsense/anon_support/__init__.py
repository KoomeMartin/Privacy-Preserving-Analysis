"""Local copy of minimal depth-anon logic for standalone `realsense/` scripts.

Vendored from ``src/depth_anon/`` so this folder runs without the parent repo on
``PYTHONPATH``. When changing blur / detection behaviour, sync with the main package.
"""
